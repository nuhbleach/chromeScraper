A small POC script that will pull the first 20 screen captures out of google chrome and save them in your /tmp. The images will also be displayed using image majic. Only tested under Ubuntu but can be extended to work anywhere (know your python folks). To use run with -u your_user_name.
